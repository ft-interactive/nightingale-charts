test:
  post:
    - git config --global user.name "circleci"
    - git config --global user.email "jonathan.best@ft.com"
general:
  artifacts:
    - test/coverage
machine:
  timezone:
    Europe/London
  node:
    version: v0.10.33
